{
  "name": "sweepy",
  "version": "0.1.0",
  "private": true,
  "description": "AI Email Management Agent",
  "scripts": {
    "dev:web": "npm run dev --prefix web",
    "dev:ext": "npm run dev --prefix extension",
    "build:web": "npm run build --prefix web",
    "build:ext": "npm run build --prefix extension",
    "lint": "npm run lint --prefix web && npm run lint --prefix extension",
    "typecheck": "npm run typecheck --prefix web && npm run typecheck --prefix extension"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
